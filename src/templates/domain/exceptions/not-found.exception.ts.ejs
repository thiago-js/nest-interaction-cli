import { BadRequestException, HttpStatus } from "@nestjs/common";

export class NotFound<%= props.nameCapitalize %>Exception extends BadRequestException {
  constructor(message: any) {
    super({
      statusCode: HttpStatus.NOT_FOUND,
      message,
    });
  }
}
