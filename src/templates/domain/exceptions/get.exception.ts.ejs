import { BadRequestException, HttpStatus } from "@nestjs/common";

export class Get<%= props.nameCapitalize %>Exception extends BadRequestException {
  constructor(message: any) {
    super({
      statusCode: HttpStatus.BAD_REQUEST,
      message,
    });
  }
}
